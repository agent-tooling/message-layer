# Secrets — never commit environment files or credential-bearing configs.
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
credentials.json
secrets.json

# Build / runtime artifacts.
node_modules
dist
coverage
*.log

# Local database / cache files.
.pglite
.data/
agents/**/.data/
clients/**/.data
clients/**/.data.*

# Reference repos (sparse clones, not tracked).
references/electric/
references/json-render/
